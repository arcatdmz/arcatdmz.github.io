doctype html
- const url = `${protocol}://${domain}${rootPath}user-generated-variables`;
html(lang='en')
  head
    meta(http-equiv='reresh', content=`0; URL='${url}'`)
  body
    p: a(href=url) This URL is a shorthand for the full URL (#{url})