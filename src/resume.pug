include ./resume/_style
include ./resume/_menu
include ./resume/_profile
include ./resume/_work
include ./resume/_grants
include ./resume/_education
include ./resume/_honors
include ./resume/_publications
include ./resume/_innovations
include ./resume/_services

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='author', content='Jun Kato')
    meta(name='viewport', content='width=device-width,user-scalable=yes')
    link(rev='made', href='mailto:i@junkato.jp')
    title Resume of Jun Kato
    +style()
  body
    #menu: +menu()
    #container
      p#updated Updated: 8/19/2023
      #profile: +profile()
      #work: +work()
      #grants: +grants()
      #education: +education()
      #honors: +honors()
      #publications: +publications()
      #innovations: +innovations()
      #services
        +services()
        section#services-oc
          .row
            .year
            .content: h3 Organizing Committee
          - for (let member of misc.services.members) {
          .row
            .year
            .content
              - for (let role of member.role) {
              | #{role.title}
              | (#{role.from}-#{role.to}), 
              - }
              a(href=member.url): strong #{member.name}
          - }
